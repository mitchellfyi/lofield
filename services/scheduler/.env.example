# Lofield FM Scheduler Service Environment Variables

# Database Connection
# This should match the DATABASE_URL in web/.env
DATABASE_URL="postgresql://lofield:CHANGE_ME_TO_SECURE_PASSWORD@localhost:5432/lofield_fm?schema=public"

# Scheduler Configuration
# How far ahead to maintain the broadcast queue (in minutes)
# Default: 45 minutes
SCHEDULER_BUFFER_MINUTES=45

# Minimum queue depth before triggering content generation (in minutes)
# Default: 15 minutes
MIN_QUEUE_DEPTH_MINUTES=15

# How often to check the queue and generate content if needed (in seconds)
# Default: 60 seconds (1 minute)
SCHEDULER_CHECK_INTERVAL=60

# Audio Storage Path
# Where to store generated audio files
# Default: /tmp/lofield/audio
AUDIO_STORAGE_PATH=/tmp/lofield/audio

# Archive Path
# Where to store hourly archive files for time-shifted listening
# Default: /tmp/lofield/archive
ARCHIVE_PATH=/tmp/lofield/archive

# ============================================================================
# AI Service Configuration
# ============================================================================

# OpenAI API Configuration
# REQUIRED: Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Script Generation Settings
# Model to use for script generation (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
SCRIPT_MODEL=gpt-4o-mini

# Temperature for script generation (0.0-2.0, default: 0.7)
# Higher = more creative, Lower = more focused
SCRIPT_TEMPERATURE=0.7

# Text-to-Speech (TTS) Configuration
# Provider: openai or elevenlabs (default: openai)
TTS_PROVIDER=openai

# OpenAI TTS Model (if using openai provider)
# Options: tts-1 (faster), tts-1-hd (higher quality)
TTS_MODEL=tts-1

# ElevenLabs Configuration (only if TTS_PROVIDER=elevenlabs)
# Get your API key from https://elevenlabs.io
ELEVENLABS_API_KEY=

# Music Generation Configuration (future use)
# Replicate API token for MusicGen
# Get your token from https://replicate.com
REPLICATE_API_TOKEN=

# Stability AI API key for music generation
STABILITY_AI_API_KEY=

# Node Environment
NODE_ENV=development
